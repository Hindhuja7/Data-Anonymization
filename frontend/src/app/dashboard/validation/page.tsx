import { ComingSoon } from '@/components/dashboard/coming-soon';

export default function ValidationPage() {
  return (
    <ComingSoon
      featureName="Validation Engine"
      description="Validate anonymized data quality with statistical tests, format verification, and referential integrity checks."
    />
  );
}
