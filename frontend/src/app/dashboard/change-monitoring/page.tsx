import { ComingSoon } from '@/components/dashboard/coming-soon';

export default function ChangeMonitoringPage() {
  return (
    <ComingSoon
      featureName="Change Monitoring"
      description="Track incremental changes in your source database and trigger selective re-anonymization for modified records."
    />
  );
}
