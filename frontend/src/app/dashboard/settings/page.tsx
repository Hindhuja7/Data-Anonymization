import { ComingSoon } from '@/components/dashboard/coming-soon';

export default function SettingsPage() {
  return (
    <ComingSoon
      featureName="Settings"
      description="Configure application settings including database connections, API keys, notification preferences, and security options."
    />
  );
}
